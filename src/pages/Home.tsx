import { Container } from '@mantine/core';
import Users from '../components/Users';

const Home = () => {
  return (
    <Container size="30rem" px={0}>
      <Users />
    </Container>
  );
};

export default Home;
